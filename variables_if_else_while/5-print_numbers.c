#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int number = 0;

	for (; number <= 9; number++)
	{
		printf("%d", number);

	}
	putchar('\n');
	return (0);
}
