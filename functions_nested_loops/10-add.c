#include "main.h"

/**
 * add - add two number
 * @a : first digit
 * @b : second digit
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */

int add(int a, int b)
{
	int result = a + b;

	return (result);
}
